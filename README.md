# Employee-Management
The Employee Management System is a full-stack web application developed using Angular for the frontend and Spring Boot for the backend, with SQL used as the relational database for data storage. The application provides secure login and registration functionality, allowing authenticated users to access the system.
